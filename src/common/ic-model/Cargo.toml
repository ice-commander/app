[package]
name = "ic-model"
version = "0.1.0"
edition = "2021"
description = "Plain data types shared between the file manager's crates"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_bytes = "0.11"

[dev-dependencies]
serde_json = { workspace = true }
